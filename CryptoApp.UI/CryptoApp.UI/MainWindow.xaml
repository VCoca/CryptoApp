<Window x:Class="CryptoApp.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CryptoApp.UI"
        mc:Ignorable="d"
        Title="Crypto File Transfer" Height="760" Width="950"
        Background="#1E1E1E" Foreground="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Encryption Settings" Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" Margin="10" VerticalAlignment="Center">
                
                <TextBlock Text="Algorithm:" Margin="0,0,5,0"/>
                <ComboBox Width="130" x:Name="AlgorithmBox"
                          SelectionChanged="AlgorithmBox_SelectionChanged">
                    <ComboBoxItem Content="RC6"/>
                    <ComboBoxItem Content="Playfair"/>
                </ComboBox>

                <TextBlock Text="Mode:" Margin="15,0,5,0"/>
                <ComboBox Width="130" x:Name="ModeBox">
                    <ComboBoxItem Content="ECB"/>
                    <ComboBoxItem Content="PCBC"/>
                </ComboBox>

                <TextBlock Text="Key:" Margin="15,0,5,0"/>
                <PasswordBox Width="160" x:Name="KeyBox"/>

                <TextBlock Text="Use SHA1:" Margin="15,0,5,0"/>
                <CheckBox x:Name="UseSHABox"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="File Watcher" Grid.Row="1" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" Margin="10">
                
                <TextBox Width="330" x:Name="WatchFolderBox" IsReadOnly="True"/>
                <Button Content="Browse" Margin="5,0" Click="Browse_Click"/>
                
                <Button Content="Start Watching" Margin="15,0"
                        x:Name="StartWatcherBtn"
                        Click="StartWatcher_Click"/>
                
                <Button Content="Stop"
                        x:Name="StopWatcherBtn"
                        Margin="5,0" 
                        IsEnabled="False"
                        Click="StopWatcher_Click"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Output Folder" Grid.Row="2" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBox Width="330" x:Name="OutputFolderBox" IsReadOnly="True"/>
                <Button Content="Browse" Margin="5,0" Click="BrowseOutput_Click"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Manual Encoding (when watcher is OFF)" Grid.Row="3" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" Margin="10">
                <Button Content="Select File and Encode"
                        x:Name="ManualEncodeBtn"
                        Click="ManualEncode_Click"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Logs" Grid.Row="4">
            <ListBox x:Name="LogList"/>
        </GroupBox>
    </Grid>
</Window>
